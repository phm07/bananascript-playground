{
  "name": "bananascript-playground",
  "version": "1.0.0",
  "scripts": {
    "lint": "npm --prefix ./backend run lint && npm --prefix ./frontend run lint",
    "startBackend": "npm --prefix ./backend run start",
    "startFrontend": "npm --prefix ./frontend run start",
    "build": "node build.js"
  },
  "dependencies": {
    "fs-extra": "^11.1.0"
  }
}
